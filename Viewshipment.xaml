<Window x:Class="ShipmentManagement.Viewshipment"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShipmentManagement"
        mc:Ignorable="d"
        Title="Viewshipment" Height="450" Width="800">
    <ListView ItemsSource="{Binding shipmentCollection}">

        <ListView.View>

            <GridView AllowsColumnReorder="true" ColumnHeaderToolTip="Shipment Information">

                <GridViewColumn DisplayMemberBinding="{Binding Path=Shipment_type}" Header="Shipment Type" Width="100"/>

                <GridViewColumn DisplayMemberBinding="{Binding Path=Origin}" Header="Origin" Width="100"/>

                <GridViewColumn DisplayMemberBinding="{Binding Path=Destination}" Header="Destination" Width="100"/>

                <GridViewColumn DisplayMemberBinding="{Binding Path=Status}" Header="Status" Width="100"/>
            </GridView>

        </ListView.View>
    </ListView>
</Window>
